<%= form_for(@risp_service) do |f| %>
  <% if @risp_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@risp_service.errors.count, "error") %> prohibited this risp_service from being saved:</h2>

      <ul>
      <% @risp_service.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :munit %><br />
    <%= f.text_field :munit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
